<h1>Check Out Family Time</h1>

<section class="container-fluid">
  <% @products.each do |product| %>
    <%= render partial: "product", locals:{ product:product} %>

  <% end %>
</section>

 <section id="new_product_link">
  <%= link_to 'New Product', new_product_path %>
</section>